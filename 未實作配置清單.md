# Config.js 未實作功能參數清單

## 概述
本文件列出 `js/config.js` 中定義但尚未在程式碼中實際使用的配置參數。

**最後更新**: 2024年（已移除已實作的配置項）

---

---

## 統計摘要

### 完全未實作（0 項）
無

### 部分實作（0 項）
無

### 總計
- **完全未實作**: 0 項
- **部分實作**: 0 項
- **總計**: 0 項未完整實作的配置參數

**所有配置參數已完整實作！** 🎉

---

## 已實作項目（已移除）

以下配置項已經實作，已從清單中移除：

### 優先實作項目（已完成）
1. ✅ `calendar.dayMinHeight` - 已實作（通過 `applyDayMinHeight()` 方法動態設置 CSS）
2. ✅ `calendar.taskBarHeight` - 已實作（在 `createSpanningTaskBar()` 中使用配置值）
3. ✅ `taskDisplay.showSpanningTasks` - 已實作（在 `render()` 方法中檢查，禁用時在日期格子內顯示任務）
4. ✅ `taskDisplay.showPlaceholders` - 已實作（在 `createSpanningTaskBar()` 中檢查）
5. ✅ `taskDisplay.hoverEffect` - 已實作（在 `createSpanningTaskBar()` 中添加 `hover-enabled` 類別）
6. ✅ `dateFormat.display` - 已實作（在 `formatDate()` 方法中使用配置的格式）

### 次要實作項目（已完成）
7. ✅ `calendar.maxDisplayTasks` - 已實作（在 `renderTasksInDayCells`、`renderSpanningTasks` 等方法中限制任務數量）
8. ✅ `calendar.maxTasksInBlock` - 已實作（在 `renderTasksInDayCells` 中按環境分組限制任務數）
9. ✅ `legend.maxItemsPerRow` - 已實作（在 `createLegend()` 中使用 CSS Grid 控制每行項目數）
10. ✅ `filter.showAllButton` - 已實作（在 `createEnvironmentFilters()` 中根據配置控制按鈕顯示）

### 進階功能（已完成）
11. ✅ `performance.virtualScroll` - 已實作（使用 IntersectionObserver 監控可見區域，只渲染可見的日期格子）

### 完善部分實作項目（已完成）
12. ✅ `dateFormat.input` - 已完善（在 `parseDate()` 和 `parseDateByFormat()` 方法中完全遵循配置的日期格式）
13. ✅ `debug.logLevel` - 已實作（創建 `Logger` 工具類別，根據配置的日誌級別過濾日誌輸出）

---

## 備註

- 本清單基於 2024 年程式碼版本
- 部分配置可能在其他分支或未來版本中已實作
- 建議定期更新此清單以反映最新狀態
